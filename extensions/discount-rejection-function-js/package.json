{
  "name": "discount-rejection-function-js",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "../../node_modules/.bin/esbuild --bundle --format=esm --target=es2020 --outfile=dist/function.js --external:ShopifyFunction node_modules/@shopify/shopify_function/run.ts src/cart_lines_discounts_generate_run.js",
    "shopify": "npm exec -- shopify",
    "typegen": "npm exec -- shopify app function typegen",
    "preview": "npm exec -- shopify app function run",
    "test": "vitest",
    "test:unit": "vitest run src/"
  },
  "codegen": {
    "schema": "schema.graphql",
    "documents": "src/*.graphql",
    "generates": {
      "./generated/api.ts": {
        "plugins": [
          "typescript",
          "typescript-operations"
        ]
      }
    },
    "config": {
      "omitOperationSuffix": true
    }
  },
  "dependencies": {
    "@shopify/shopify_function": "^2.0.1"
  },
  "devDependencies": {
    "@shopify/shopify-function-test-helpers": "^1.0.0",
    "vitest": "^3.2.4"
  }
}
